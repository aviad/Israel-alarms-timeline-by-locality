[project]
name = "alarms-graph"
version = "0.1.0"
description = "Israel rocket alert frequency chart — Cloudflare Worker"
requires-python = ">=3.12"
dependencies = [
    "matplotlib",
]

[dependency-groups]
dev = [
    "pywrangler",
]
